{% block style %}
    <style>
        label {
            font-weight: 600;
        }

        input {
            width: 100%;
            height: 30px;
        }
    </style>
{% endblock %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


